<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:clickable="true" >

    <RelativeLayout
        android:id="@+id/relreceiptlist_1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/listback" >

        <TextView
            android:id="@+id/receipt_list"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:padding="16dp"
            android:text="RECEIPTS"
            android:textColor="#FFF"
            android:textSize="22sp" />

        <Button
            android:id="@+id/butreceiptlist_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:padding="@dimen/activity_horizontal_margin"
            android:text="BACK"
            android:textColor="#FFF"
            android:textSize="14sp" />
    </RelativeLayout>

    <ListView
        android:id="@+id/listreceiptlist"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/viewreceipt_1"
        android:layout_below="@+id/relreceiptlist_1"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:background="@drawable/receiptlist_papa_rect"
        android:divider="@color/margincolor"
        android:dividerHeight="0dp"
        android:footerDividersEnabled="false"
        android:headerDividersEnabled="false"
        android:overScrollMode="never"
        android:scrollbars="none" >
    </ListView>

    <View
        android:id="@+id/viewreceipt_1"
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_alignParentBottom="true"
        android:background="#000" />

</RelativeLayout>